create a shared app shell component that wraps all pages

needs:
- glass header with logo and nav
- nav items: Tasks (worker feed), Composer, Management, Submissions (last 3 are admin)
- active state highlighting
- mobile responsive - collapse labels on small screens, keep icons

use the glass effect from the design system. header should be sticky

put a little "Demo Mode" indicator somewhere so evaluators know data is mocked
